# Coin Coding PRD V1.1

> 一句话定位：一个 macOS 菜单栏小游戏。当 Claude Code 在干活时，屏幕上飘落金币，用户点击收集，并在全球排行榜上与其他开发者比拼。

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | — | 初稿（ChatGPT 生成） |
| V1.1 | 2026-06-10 | 全面修订：改用 hooks 集成、确定菜单栏应用形态、强制 GitHub 登录、补充防作弊/数值/成功指标 |

---

## 1. 背景与要验证的假设

使用 Claude Code 进行 Vibe Coding 时，用户经常需要等待数十秒至数分钟。等待期间：不方便离开、不想切换应用、过程无反馈。

**核心假设（本产品唯一要验证的事）：**
用户愿意在 Claude Code 工作的等待期间，通过简单的点击小游戏消磨时间。

### 成功指标（上线前定死，上线一个月后对照）

| 指标 | 标准 | 验证什么 |
|------|------|----------|
| 激活率 | 装机用户中 ≥ 40% 累计点击超过 50 枚金币 | 装了之后真的玩了吗 |
| 7 日留存 | 装机满 7 天的用户中 ≥ 20% 在第 7 天仍有点击 | 新鲜感过后还在玩吗 |
| 排行榜打开率 | ≥ 30% 的用户打开过排行榜 | 排行榜是不是真的核心吸引力 |

达标 → 投入做 V2；未达标 → 承认假设不成立，止损。

---

## 2. 目标用户

**核心用户：** 极客向的 Claude Code 高频用户 —— AI 开发者、独立开发者、重度 Vibe Coding 用户。默认人人有 GitHub 账号。

**非目标用户：** 普通 Claude Chat 用户、非 Coding 场景用户、无 GitHub 账号的用户。

**平台：** V1.0 仅支持 macOS。Windows / Linux 明确不支持。

---

## 3. 产品形态

**macOS 菜单栏应用（menu bar app）：**

- 无主窗口、无 Dock 图标，仅菜单栏一个小图标（查看金币数、设置、退出）
- 开机自启，装完即忘
- 平时完全隐形；只有 Claude Code 开始工作时，悬浮游戏层才出现
- **不提供独立启动器**：用户照常使用 Claude Code，无需改变任何习惯（V1.0 的 `coincode` 启动器方案已废弃）

---

## 4. 与 Claude Code 的集成（技术核心）

**采用 Claude Code 官方 hooks 机制**，不监听进程或输出流。

- 安装时向用户的 Claude Code 配置写入 hook：任务开始、任务结束、等待用户批准等时刻，Claude Code 主动向本应用发送信号
- 安装时明确告知用户会修改该配置；卸载时完整清理

**状态规则：**

| Claude 状态 | 游戏行为 |
|-------------|----------|
| 开始工作（hook 信号） | 游戏开始，金币掉落 |
| 结束工作（hook 信号） | 游戏立即停止，无缓冲时间 |
| 暂停等待用户批准操作 | **金币暂停掉落**（既不鼓励干等，金币停了本身也是"Claude 在等你"的优雅提醒） |
| 多个 Claude Code 会话同时工作 | 不叠加。任一会话在工作即正常掉落，全部停止才停止（防多开刷分） |

游戏起止与 Claude 实际工作区间完全同步，无需 Idle 超时判断。

---

## 5. 核心玩法与数值

- 悬浮层内金币从上向下飘落，鼠标点击收集，+1 Coin
- 未点中的金币落出后直接消失，**无惩罚**
- 金币累计进入账户总资产（Total Coins，永久累计）

**初始数值（上线后可远程调整，无需发版）：**

| 参数 | 初始值 | 设计理由 |
|------|--------|----------|
| 掉落频率 | 每 1.5 秒 1 枚 | 游戏是等待时的"小零食"而非正餐（用户试玩后由 2 秒调密） |
| 单枚下落时长 | 约 6 秒 | 慢节奏，不焦虑（用户试玩后由 4 秒调慢） |
| 大金币 | 5% 概率出现，价值 10 Coins | 低成本的小惊喜，点击游戏留人的经典配方 |

**爽感机制（均为克制的文字化反馈，符合代码风设计语言）：**

- **连击**：连续接住不漏显示 `streak xN`，≥10 变绿色；漏接清零。点击音调随连击数爬升。
- **整百里程碑**：金币总数跨过整百时，飘字显示 `// 300!`。
- **收工结算**：Claude 每轮工作结束，状态栏显示本轮所得 `// session +N`。

---

## 6. 账号系统

**强制 GitHub 登录，不支持匿名。**

- 首次启动引导 GitHub OAuth 登录，登录后才能开始游戏
- 设计依据：目标用户全是极客，人手 GitHub，登录摩擦极低
- 带来的简化：无需匿名 ID 体系、排行榜直接展示 GitHub 用户名和头像（对开发者有真实炫耀价值）、跨设备资产天然同步、封禁作弊账号成本高（刷小号不再零成本）

> 注：与 Claude 账号打通不可行 —— Anthropic 未向第三方开放账号授权。

---

## 7. 排行榜与防作弊

排行榜为**核心功能**。展示全球榜（按 Total Coins 降序）+ 个人当前排名。

**防作弊原则：** 目标用户全是程序员，客户端上报的数据默认不可信。不追求杜绝作弊，追求让假数据混不进榜单。

**服务器裁判规则：**

1. **人类点击上限** —— 单位时间金币增量超过人类物理点击速度（约 5–6 次/秒）即判无效
2. **工作时长对账** —— 客户端同时上报 Claude 工作起止信号；金币总量必须与该用户的 Claude 实际工作时长 × 掉落速率相匹配，超出即判无效
3. 保留手动封禁异常账号的能力（圈子小，肉眼可查）

---

## 8. 界面设计

**悬浮游戏层：**

- 默认很小，背景完全透明，仅可见：飘落的金币 + 累计数值（如 `Coins: 125`）
- 用户可自由**调整窗口大小和位置**（默认右上角）
- **点击穿透**：仅金币本身响应点击，其余区域鼠标直接穿透到下层应用，不妨碍任何正常操作
- **全屏兼容**：悬浮层位于系统最高窗口层级，Claude Code / 终端全屏时仍正常显示和交互

**菜单栏：** 图标 + 下拉面板（当前金币数、排行榜入口、设置、退出）。

**排行榜页：** 全球排名列表（GitHub 头像 + 用户名 + Total Coins）、本人当前排名高亮。

---

## 9. 数据统计与隐私

**采集（仅用于计分、防作弊与假设验证）：**

- 游戏数据：启动次数、游戏时长、点击次数、获得金币数、排行榜打开次数
- Claude 数据：工作时段的**起止时间**、启动次数

**隐私承诺（首次启动明示）：**

> 本游戏仅记录 Claude Code 的工作起止时间用于计分，**不读取、不上传任何对话内容和代码**。

---

## 10. 技术方案

| 模块 | 选型 | 理由 |
|------|------|------|
| 客户端 | Swift 原生（菜单栏应用） | 常驻内存极小（约十几 MB）；透明悬浮窗、点击穿透、置顶于全屏应用，原生支持最好。明确不用 Electron（空壳即占一两百 MB，与"轻量附属"定位矛盾） |
| Claude 集成 | Claude Code hooks | 官方机制，零侵入，事件精确 |
| 后端 | Supabase | 用户信息、金币、排行榜；金币入账走服务端校验逻辑（见第 7 节），客户端不可直写分数 |
| 登录 | GitHub OAuth | 见第 6 节 |
| 数值配置 | 服务端远程下发 | 掉落频率等参数可在线调整，无需发版 |

---

## 11. MVP 范围

**V1.1 包含：**

- 菜单栏应用 + hooks 状态集成
- 金币掉落 / 点击收集 / 累计
- GitHub 登录
- 全球排行榜 + 个人排名
- 服务端防作弊校验
- 基础数据统计 + 隐私告知

**明确不包含：** 道具、商店、成就、好友、聊天、多种货币、复杂玩法、Windows/Linux、账号迁移之外的任何账号功能。

**唯一目标：** 验证第 1 节的核心假设。
