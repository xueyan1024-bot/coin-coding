# Coin Coding 项目备忘录

这是给 Claude 的项目上下文。新会话开始前先读 [PRD.md](PRD.md)，它是唯一的需求来源（V1.1，已经过逐条讨论确认）。

## 关于用户

- 用户**不是开发者**，看不懂技术术语。沟通时用大白话和比喻解释技术概念，避免黑话。
- 用户偏好**一次只讨论一个问题**，逐个确认，不要一次抛出大量信息。
- 重要决定要让用户拍板，但数值类、纯技术类细节用户授权"帮我定最合理的"。

## 项目状态（截至 2026-06-10）

- PRD V1.1 已定稿（由 V1.0 全面修订而来）。
- `demo/` 网页演示版已完成并在电脑 A 验证通过：hooks 状态联动（working/paused/idle）+ 核心玩法全部正常。
- `native/` Swift 原生版代码已写好但**尚未编译**（电脑 A 缺 Xcode 命令行工具）。
- **下一步（在有 Xcode 工具的电脑上）**：运行 `bash native/build.sh` 编译并测试原生版，重点验证：置顶于全屏应用、点击穿透、不抢终端焦点。hooks 已由 `demo/setup_hooks.py` 安装（电脑 B 需重新运行它一次）。
- 之后的大块工作：GitHub 登录、Supabase 后端、排行榜、防作弊（见 PRD 第 6/7 节）。

## 已定死的关键决策（不要重新打开讨论，除非用户主动提出）

1. **集成方式**：Claude Code hooks，不做 `coincode` 包装启动器（原 V1.0 方案已废弃，包装交互式 CLI 风险大）。
2. **产品形态**：macOS 菜单栏应用，Swift 原生，明确不用 Electron。
3. **账号**：强制 GitHub 登录，无匿名模式。与 Claude 账号打通不可行（Anthropic 未开放）。
4. **排行榜**：核心功能，必须保留。防作弊靠服务端两条规则：人类点击速度上限 + Claude 工作时长对账。
5. **数值**：每 2 秒 1 枚金币、下落 4 秒、漏接无惩罚、5% 大金币（10 分）；所有数值服务端可远程调整。
6. **状态同步**：与 Claude 工作区间完全同步无缓冲；等待用户批准时暂停掉币；多会话不叠加。
7. **悬浮窗**：小、透明、仅金币和计数可见；用户可调大小和位置；金币外区域点击穿透；置顶于全屏应用之上。
8. **成功指标**：激活率 ≥40%（点击 >50 枚）、7 日留存 ≥20%、排行榜打开率 ≥30%。
