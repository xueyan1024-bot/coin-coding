# Coin Coding 项目备忘录

这是给 Claude 的项目上下文。新会话开始前先读 [PRD.md](PRD.md)，它是唯一的需求来源（V1.1，已经过逐条讨论确认）。

## 关于用户

- 用户**不是开发者**，看不懂技术术语。沟通时用大白话和比喻解释技术概念，避免黑话。
- 用户偏好**一次只讨论一个问题**，逐个确认，不要一次抛出大量信息。
- 重要决定要让用户拍板，但数值类、纯技术类细节用户授权"帮我定最合理的"。

## 项目状态（截至 2026-06-11）

- PRD V1.1 已定稿（由 V1.0 全面修订而来）。
- `demo/` 网页演示版已完成并在电脑 A 验证通过：hooks 状态联动（working/paused/idle）+ 核心玩法全部正常。
- `native/` Swift 原生版已在电脑 B 编译通过并运行。UI 已对齐代码风设计语言（此前原生版还是旧游戏风，靠用户肉眼发现——以后改 UI 要 demo 和 native 一起改）。新增：窗口灰色细描边标出边界、右下角斜纹把手拖拽改大小（顶边固定、最小 160×240、尺寸自动记住）、HUD 无背景纯文字。电脑 B 的 hooks 已安装。
- 原生版已补上声音（连击五声音阶 + 漏接低音，与 demo 同参数）、整窗拖动、HUD 右对齐。注意原生版尚无 streak 显示/里程碑/收工结算的视觉反馈（demo 有），只有声音部分。
- **待用户验证**：置顶于全屏应用、不抢终端焦点（把手改大小已验证；点击穿透已废弃，无需验证）。
- 注意：无边框 + 不抢焦点的 NSPanel 不支持系统自带的边缘拖拽改大小（.resizable 无效），必须自画把手。
- 之后的大块工作：GitHub 登录、Supabase 后端、排行榜、防作弊（见 PRD 第 6/7 节）。

## 已定死的关键决策（不要重新打开讨论，除非用户主动提出）

1. **集成方式**：Claude Code hooks，不做 `coincode` 包装启动器（原 V1.0 方案已废弃，包装交互式 CLI 风险大）。
2. **产品形态**：macOS 菜单栏应用，Swift 原生，明确不用 Electron。
3. **账号**：强制 GitHub 登录，无匿名模式。与 Claude 账号打通不可行（Anthropic 未开放）。
4. **排行榜**：核心功能，必须保留。防作弊靠服务端两条规则：人类点击速度上限 + Claude 工作时长对账。
5. **数值**：每 0.8 秒 1 枚金币（2026-06-11 用户反馈 1.5 秒太稀，小窗口下守株待兔）、下落 6 秒、漏接无惩罚、5% 大金币（10 分）；爽感机制：连击 streak（漏接清零、音调爬升）、整百里程碑、收工结算 session +N；所有数值服务端可远程调整。
6. **状态同步**：与 Claude 工作区间完全同步无缓冲；等待用户批准时暂停掉币；多会话不叠加。
7. **悬浮窗**：小、透明、仅金币和计数可见；用户可调大小（右下角斜纹把手）和位置（框内任意处按住拖动）；置顶于全屏应用之上。~~金币外区域点击穿透~~ 已废弃：2026-06-11 用户主动拍板放弃穿透，换取框内任意位置可拖动窗口。
8. **成功指标**：激活率 ≥40%（点击 >50 枚）、7 日留存 ≥20%、排行榜打开率 ≥30%。
9. **设计语言**：极简 + 代码风（终端美学）。深色编辑器底、等宽字体、金币是带同色圆圈描边的琥珀色 `$`（大金币为绿色圆圈 `$10`）、文案写成命令行样式（如 `claude: working`、`> leaderboard`）；禁止渐变、发光、粒子、抖动等游戏化特效（用户先后否决过游戏化 UI 和浅色极简 UI 两版）。demo/index.html 是风格基准。
